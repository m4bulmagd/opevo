/**
 * @deprecated Visual layout preferences were removed in favor of the curated
 * Presvo product theme. Kept temporarily as a compatibility export for the
 * existing dashboard shell test boundary.
 */
export function LayoutControls() {
  return null;
}
